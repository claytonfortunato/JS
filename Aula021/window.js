(function(win) {
    'use strict';

    if( prompt( 'Pergunta?' ) )
        console.log( 'resposta ok!' );
    else 
        console.log( 'resposta não ok!' );
    console.log( 'sempre' );
})(window);